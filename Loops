<!DOCTYPE html>
<html lang="en">
<head>
	<meta charset="utf-8">
	<title>CIT 261 - Examples of Loops</title>
	
	<script type="text/javascript">
		function forLoop(){
			var text = "";
			for (i = 0; i < 5; i++) { //for loop will add 1 to variable "i" until it reaches 5
				var random = Math.floor((Math.random() * 50) + 1); //Random number generator
				if (i == 4){ //if, else to add commas or not
				text += random;
				} else{
				text += random + ", ";				
				}
				document.getElementById('outputDiv').innerHTML = text;//output to html body
			}
		}
		
		function whileLoop(){
			var first = parseFloat(document.getElementById('first').value);
			var last = parseFloat(document.getElementById('last').value);
			var text = "";
				while(first < last)
					{
						if (first + 2 >= last){
							return;
						} 
						else {
							first = first + 2;
							text += first + " ";
						}
						document.getElementById('outputDiv2').innerHTML = text;//output to html body
					}
		}
		
		function whileDoLoop(){
			var text = "";
			var x = 0;
			do{
				text += x + " ";
				x = x + 5;
				document.getElementById('outputDiv3').innerHTML = text;//output to html body
			} while (x<=50) //stops loop at 50

		}
		
	</script>
	
</head>

<body>
	<header>
		<h1>CIT 261 - Kailey Dilworth</h1>
		<h2>Application Development - Examples of Loops</h2>
	</header>
		<h2>Loops:</h2>
		<h3>for loop:</h3>
		<button type="button" onclick="forLoop()">Press for Random Number Generator</button>
		<br/><br/>
		<div id="outputDiv"></div>
		
		<br/>
		
		
		<h3>while loop:</h3>
		<p>Count by two's between the first and last number.<p>
		<p>First Number: <input type="text" id="first"> Last Number: <input type="text" id="last"> </p>
		<button type="button" onclick="whileLoop()">Start</button>
		<div id="outputDiv2"></div>
		
		<br/>

		
		<h3>while/do loop:</h3>
		<p> Add by 5's until number 50 is reached <p>
		<button type="button" onclick="whileDoLoop()">Start</button>
		<div id="outputDiv3"></div>
		
		<br/>

	<footer>
		<p>Copyright 2017 - Kailey Dilworth<p>
	<footer>

</body>

</html>
